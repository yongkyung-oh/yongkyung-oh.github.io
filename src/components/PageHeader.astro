---
interface Props {
  eyebrow: string;
  title: string;
  lead: string;
}
const { eyebrow, title, lead } = Astro.props;
---

<header class="rec-pagehead">
  <p class="rec-kicker">{eyebrow}</p>
  <h1>{title}</h1>
  <p>{lead}</p>
</header>
